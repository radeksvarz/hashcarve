---
trigger: glob
globs: **/*.sol
---

All Solidity code changes must strictly adhere to the formatting rules defined in the foundry.toml file. After any code modification, always execute `forge fmt` to ensure compliance before finalizing the task.